;(function (global, factory) {

    if (typeof define === 'function' && define.amd || typeof exports === 'object') {
        // do nothing in case of AMD/CJS use! this is only a dummy namespace in
        //     case someone uses this plain in the browser!
        return;

    } else {
        // create a global funykJS namespace if we are in browser environment
        global.funkyJS = factory();

    }

})(this, function () {
    /**
    @module core
    **/



    // GLOBAL NAMESPACE CREATION
    // =========================

    /**
    funkyJS 2: A JavaScript framework for functional programming

    @class funkyJS
    @static
    **/
    return {};

});